# 32-byte AES-256 key for encrypting platform credentials at rest
ENCRYPTION_KEY=

# Secret used to sign JWT authentication tokens
JWT_SECRET=

# Port the Express server listens on (default: 3000)
PORT=3000

# Path to the SQLite database file (default: ./data/rippl3fx.db)
DB_PATH=./data/rippl3fx.db
